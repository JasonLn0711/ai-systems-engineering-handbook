# Failure Modes

- Service starts manually but fails under systemd.
- Port is open locally but blocked by firewall or network policy.
- Model service fails because of permission, path, driver, or resource limits.
