# Failure Modes

- VRAM estimate ignores KV cache or concurrency.
- Local model is selected without latency, throughput, or fallback plan.
- Multi-model serving oversubscribes GPU memory or scheduling capacity.
