# Failure Modes

- Poor chunking destroys retrieval quality.
- Metadata is missing, stale, overfit, or generated at the wrong layer.
- RAG answer is confident but unsupported by retrieved context.
- SQL and knowledge-base access have inconsistent permissions.
